#!/usr/bin/env python

import string
import random

def randompass():
  chars = string.ascii_uppercase + string.ascii_lowercase + string.digits
#  size = random.randint(10,15)
  size = 15
  return ''.join(random.choice(chars) for x in range(size))
  
print("Your new passwd: " + randompass())
